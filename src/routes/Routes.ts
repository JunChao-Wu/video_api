const prefix = 'service'
const version = 'v1'

export const BASE_ROUTE = {
  upload: `${prefix}/${version}/upload`,
}

export const API = {
  upload: {
    upload_post: "",
    merge_post: "/merge",
  },
}
